10 REM *************** BOILER PROGRAM **********************
20  RADSETP = BLRSP0 - OAT * (BLRSP0 - BLRSP20) / 20
30 IF RADSETP < 32 THEN  RADSETP = 32
40 IF RADSETP > 92 THEN  RADSETP = 92
41 REM ************ STEAM VALVES **********************
41 REM V1 = 1-CON3 * 2
41 REM V2 = 1-CON3 * 2 - 100
41 IF BLDSUPT < RADSETP THEN  V1 = V1 + 0.50
41 IF V2 = 0 AND BLDSUPT > RADSETP THEN  V1 = V1 - 0.50
41 IF V1 >= 100 AND BLDSUPT < RADSETP THEN  V2 = V2 + 0.50
41 IF BLDSUPT > RADSETP THEN  V2 = V2 - 0.50
41  V1 = MAX( 0 , MIN( 100 , V1 ) )
41  V2 = MAX( 0 , MIN( 100 , V2 ) )
42 IF  NOT BOILRMOD THEN  V1 = 0
42 IF  NOT BOILRMOD THEN  V2 = 0
95 REM ************* BOILERS REQUIRED ? ***************
100 IF OAT > BLR-OAT THEN STOP BOILRMOD
110 IF OAT < BLR-OAT - 0.50 THEN START BOILRMOD
120 IF BOILRMOD THEN START BOILER ELSE STOP BOILER
130 REM ****** CIRC PUMPS ARE IN A SEPARATE PROG ***********
