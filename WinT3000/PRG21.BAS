10 REM *********** ROOM 301 ********************
11 IF+ RM301OCC THEN START RM301OM
11 IF- RM301OCC THEN START RM301OM
12 IF TIME-ON( RM301OM ) > 00:00:15 THEN STOP RM301OM
20 IF RM301OM THEN START RM301FAN
20 IF  NOT RM301OM THEN STOP RM301FAN
10 REM *********** ROOM 401 ********************
11 IF+ RM401OCC THEN START RM401OM
11 IF- RM401OCC THEN START RM401OM
11 IF+ RM401KEY THEN START RM401OM
11 REM IF+ RM401KEY AND TIME-ON( RM401OM ) < 0:0:10 THEN STOP RM401OM
12 IF TIME-ON( RM401OM ) > 00:00:15 THEN STOP RM401OM
20 IF RM401OM THEN START RM401FAN
20 IF  NOT RM401OM THEN STOP RM401FAN
